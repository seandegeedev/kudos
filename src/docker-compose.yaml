services:
  app:
    build:
      context: .
      dockerfile: apps/kudos/Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "${NEXT_PORT}:3000"
    volumes:
      - source_app:/usr/local/src/kudos
    profiles: [prod, app]
  db:
    image: postgres:17.0-bookworm
    restart: always
    user: ${SYSTEM_OWNERSHIP}
    env_file:
      - .env
    environment:
      - PGDATA=/var/lib/postgresql/data/kudos
    ports:
        - 5432:5432
    volumes:
        - ../data/db:/var/lib/postgresql/data/
    profiles: [prod, db-only, migrate]
  pgadmin:
    image: dpage/pgadmin4
    restart: always
    user: ${SYSTEM_OWNERSHIP}
    ports:
      - "${PGADMIN_PORT}:8080"
    env_file:
      - .env
    environment:
      - "PGADMIN_LISTEN_PORT=8080"
      - "PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}"
      - "PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}"
      - "PGADMIN_CONFIG_LOGIN_BANNER='Kudos DB. Authorized users only.'"
    volumes:
      - ../data/pgadmin:/var/lib/pgadmin
    profiles: [prod, db-only]
volumes:
  source_app: